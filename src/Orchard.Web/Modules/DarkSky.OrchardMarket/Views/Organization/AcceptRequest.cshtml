@{
    var user = Model.User;
    var organization = Model.Organization;

    Layout.Title = T("Request Accepted");
    Layout.TitleZone.Add(New.Content_Title());
}
<p>@T("User {0} has been added to {1}!", user.DisplayName, organization.Name)</p>